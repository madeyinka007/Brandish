MONGODB_URI='mongodb+srv://BrandAuthority:!Alas2334%40__Qweb@wt-brandish.cosua6n.mongodb.net/?appName=wt-brandish'
JWT_SECRET='kte8SJwAF2O0PjhZ9IozZ8BNdW2TB0jlJaFFaQBtbdY='    # Must match NEXTAUTH_SECRET in web/.env.local
RECAPTCHA_SECRET= 'XE*__DWm#5S]yNZ-mjS3GB*F*C_,*P'           # Google reCAPTCHA v3 secret key

# --- Plain String in SSM (not secrets) ---
SES_FROM_EMAIL=              # Verified SES sending address
ADMIN_ALERT_EMAIL=           # Editor notification address
CLOUDFRONT_DIST_ID='E7MSTPTK3H66F'         # CloudFront distribution ID
S3_BUCKET_NAME='www.brandish.com.ng'            # Media upload bucket name
CF_DOMAIN='d3868v8k8pzh2k.cloudfront.net'                   # CloudFront domain e.g. d1abc.cloudfront.net
DYNAMO_DEDUP_TABLE= 'wtb_view_dedup'         # DynamoDB table name: view_dedup
DYNAMO_RATELIMIT_TABLE= 'wtb_ratelimit'     # DynamoDB table name: ratelimit
DYNAMO_REFRESH_TABLE='wtb_token'
API_BASE_URL= 'http://localhost:3001'               # This APIs own public API Gateway URL (or http://localhost:3001 locally)
FRONTEND_URL= 'http://localhost:3000'              # Public frontend URL (same value as NEXTAUTH_URL in web/.env.local)

# --- Not SSM-resolved; also needed at runtime ---
AWS_REGION='us-east-1'             # e.g. af-south-1 (Africa — Lagos) or us-east-1
NODE_ENV='development'
# Local dev: keep refresh tokens in-process instead of DynamoDB (no AWS needed for auth).
# Remove/blank this in production so DynamoDB is used. See server/lib/dynamo.ts.
AUTH_STORE=memory
